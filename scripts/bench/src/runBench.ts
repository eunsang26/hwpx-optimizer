#!/usr/bin/env node

const usage = `HWPX Phase A measurement bench (stub)

Usage:
  npm run bench -- rd --profile balanced|aggressive
  npm run bench:spike
  npm run bench:manifest

Commands are not implemented yet. See scripts/bench/README.md and the Phase A plan.
`;

console.error(usage.trim());
process.exit(1);
